"version": 1
"control-service":
   "hostname": "172.17.13.43"
   "port": 4524

# The dataset key below selects and configures a dataset backend (see below: aws/openstack/etc).
# All nodes will be configured to use only one backend

dataset:
   backend: "openstack"
   region: "RegionOne"
   verify_peer: false
   auth_plugin: "password"
   auth_url: ""
   username: ""
   password: ""
   tenant_name: ""
   network: ""
   
